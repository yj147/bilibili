# Backend service settings
SENTINEL_API_KEY=
SENTINEL_DEBUG=false
SENTINEL_HOST=0.0.0.0
SENTINEL_PORT=8000

# Frontend runtime settings (for Next.js route handler)
# Default is localhost. Override in WSL/special networks when needed.
BACKEND_INTERNAL_URL=http://localhost:8000

# Frontend service port in container
FRONTEND_PORT=3000
